const controllerWithTryCatch = require('./controllerWithTryCatch');
const validatePassword = require('./validatePassword');
const pagination = require('./pagination');

module.exports = {
  controllerWithTryCatch,
  validatePassword,
  pagination,
};
